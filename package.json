{
  "name": "gempa-monitoring",
  "version": "1.0.0",
  "scripts": {
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev -- --host",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "cors": "^2.8.5",
    "express": "^5.2.0",
    "xml2js": "^0.6.2"
  }
}
